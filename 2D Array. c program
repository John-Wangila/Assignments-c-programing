#include <stdio.h>

int main() {
    int scores[2][2];
    int i, j;

    // Initialize the array with the given values
    scores[0][0] = 65; scores[0][1] = 92;
    scores[1][0] = 84; scores[1][1] = 72;

    // Print the elements of the array using a nested for loop
    for (i = 0; i < 2; i++) {
        for (j = 0; j < 2; j++) {
            printf("%d ", scores[i][j]);
        }
        printf("\n");
    }

    return 0;
}
